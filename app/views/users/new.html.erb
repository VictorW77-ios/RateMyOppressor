<h1>Sign Up</h1>

<%= form_for(@user, url: '/signup') do |f| %>
    <%= f.label :email %> 
    <%= f.email_field :email, required: true %> 
    <br>
    <%= f.label :password %> 
    <%= f.password_field :password %> 

    <%= f.submit "Submit" %>
<% end %>